# Milestone 5 form expansion — bounded independent review

Reviewed 2026-09-07 EDT / 2026-09-08 UTC. **One P2 finding requires a narrow document correction.** No other actionable contradiction was found in the reviewed expansion and sampled primary evidence. This is a design-document review; the controller retains overall scope approval and implementation release authority.

Only this new report was written. No production source, grammar, tests, design, matrix, research notes, or earlier review artifacts were changed. No product tests, parser execution, fixture execution, live Splunk query, or Git mutation was performed.

## P2 — Preserve the standalone-start negative during fixture assembly

- Location: `form-expansion.md:15`, affecting `E.L01.start.N1` at `form-expansion.md:1524`.
- The assembly rule prepends `FROM main | ` to every form snippet that does not begin with a listed recognized start. The candidate `failure index=app` does not match an exception, so literal application produces `FROM main | failure index=app`.
- This changes the intended standalone-start test into a pipeline containing an unknown command. The approved start restriction is about the initial token (`design-spec.md:27`); unknown/unimplemented standalone grammar instead retains incomplete syntax coverage (`design-spec.md:77`). The altered query no longer demonstrates the advertised malformed initial-search boundary, and treating it as a definite-invalid fixture could pressure the implementation to violate unknown-command recovery.
- Correction: explicitly declare all `E.L01.start` candidates complete queries that must not receive a prefix, including deliberately invalid starts. Preserve the existing candidate text and IDs. In document-only assembly verification, assert that N1 remains exactly `failure index=app`. No production test or new scope is needed to correct this instruction.

## Checks and evidence

The expansion text, approved design, current research addendum, relevant compact matrix sections, and both prior review reports were inspected. The original matrix was used as an approved baseline; its unchanged command catalog was not broadly re-researched. New command/language rows and function signatures were read, with source verification focused on representative boundaries rather than all syntax permutations.

1. **Counts and held forms.** An independent text-only recount found 149 E sections, 34 F sections, five EH rows, and 778 E/F/I candidate IDs with no duplicate IDs. The latter is a count of obligation labels, not unique queries or semantic forms. The matrix still has 288 seed labels and 152 N/B labels; H01–H12 are present. The earlier review's byte-for-byte baseline comparison was not repeated here. Expansion lines 5, 11–17, 2578–2590 and 2671 correctly keep obligation totals and disputed variants separate from unique-fixture/definite-negative floors and executed conformance.
2. **Named calls and comparison operands.** Lines 19–23, 2039–2050 and 2560–2563 preserve colon-label recognition, positional-prefix/named-suffix ordering, list-array syntax, non-field labels, and located value reads while keeping all named-call semantics U. The primary [Naming function arguments](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-manual/functions/naming-function-arguments) page's ordering section and valid/invalid table support those distinctions; its examples also contain positional equality expressions. No equals-sign named-call rejection or generic parameter-registry promotion is introduced. Unknown/duplicate labels and named aggregate semantics remain unproved.
3. **Type predicates and SQL span holds.** E.L07.is_type and the corrected compact L07 summary distinguish type negation from null negation. The primary [Predicate expressions](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-manual/expressions-and-predicates/predicate-expressions) page separately specifies `NOT (expression IS data-type)` and `expression IS NOT NULL`; EH04 conservatively holds the unproved alternative. The [FROM syntax](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-reference/from-command/from-command-syntax) synopsis and detailed grouping-span examples do differ over assignment parentheses, supporting EH05's incomplete treatment. Neither hold is used as a definite negative.
4. **Option-specific boundaries.** E.C23's numeric/log spans and E.C30's axis/split options agree with the sampled [bin syntax/usage](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-reference/bin-command/bin-command-overview-syntax-and-usage), [timechart syntax](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-reference/timechart-command/timechart-command-overview-and-syntax), and [timechart usage](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-reference/timechart-command/timechart-command-usage) evidence. Bare numeric spans are allowed; explicit log bounds are static contract checks; split-only options follow the split field; bin options can occupy either axis-specific position; specifying both bins and span is grammatical. The wider weekly/unit-only bin vocabulary stays EH01/U. The parenthesized versus bare timechart aggregation prefix is correctly EH02, separate from H04. The expansion does not convert omitted optional arguments or ignored options into arbitrary syntax errors.
5. **Time-selector conflict.** The primary [Time modifiers](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-manual/dates-and-time/time-modifiers) page both limits comparisons in prose and uses other comparisons in an example. EH03 records that conflict rather than borrowing general field-comparison support or manufacturing invalid fixtures. Deprecated modifier forms remain outside the finite expansion/U without being declared invalid merely because deprecated.
6. **Function context and nullability.** F signatures distinguish optional and variadic arities, scalar versus aggregate min/max, and `count()` with no operand reads versus argument-bearing calls. The sampled [Aggregate functions](https://help.splunk.com/en/splunk-enterprise/search/spl2-search-reference/statistical-and-charting-functions/aggregate-functions) reference corroborates count predicate/search-literal operands and the timechart eval-wrapper example. The finite core is explicitly narrower than the native catalog; scalar min/max, extra aliases and unproved implicit labels stay U. Function names provide no universal non-nullability guarantee. Alias-free label wording in F.count must be read with the explicit `count()` limitation in the matrix/design and line 2576's unproved-label rule; no broader count-operand naming conclusion was independently established in this review.
7. **Architecture and truthful status.** The obligations preserve typed ANTLR contexts, distinct lexical categories and original source slices; they do not replace parsing with snippet assembly. Shared transfer, source-universe refinement, tombstones and known internals remain authoritative. SQL aggregate and final projection phases remain SELECT-owned, with original lexical references and actual evaluation order. Dynamic paths, lambdas, branch merges, search-literal bodies, named calls and unproved syntax retain specific incomplete outcomes. Definite contract errors need not falsely mark syntax coverage incomplete. Exact shared interfaces and additive phase metadata still depend on verified M4; this supplement does not supply an implementation plan or imply adapter conformance.

The previously corrected slash-regex seeds and H12 boundary are retained; quoted alternation does not settle slash-plus-internal-pipe syntax. Object-key rejection and expression-position search literals now have explicit expansion obligations. Their broader source audit was not repeated. Likewise, preservation of H01–H12 means preserving the prior research disposition, not independently proving every held candidate.

## Reviewed input identity and remaining limits

SHA-256 values identify the three current expansion inputs reviewed here:

| Artifact | SHA-256 |
| --- | --- |
| form-expansion.md | `4281ef7dec370f349994af7305f5230bc2bd938465fc078df0d7c8469d5e6744` |
| conformance-matrix.md | `84d07d0ba4638cff5e3e7cf021698a9d570d0b4a81f46414a670f0eb8491fe51` |
| research-notes.md | `0bdbe54e0ae5423eba312b088152dbaea164f4b5aadab8199d8d13f84fd4538d` |

The approved design also matched its earlier review hash: `05660c261d860eb0ed09e9dadbf30309ae82a01498e4fdfa144828205139b911`. Targeted primary retrievals retained the publisher update metadata recorded in the expansion ledger for the cited pages.

Resolve the P2 assembly instruction before using this expansion as fixture-generation guidance. Deduplicated durable fixtures, exact trees/references/scopes/state/phase assertions, production registry behavior, typed recovery, adapter parity and all parser/regression execution remain unverified. This report makes no claim that those fixtures exist or pass, and does not review any later edit or production HEAD.
