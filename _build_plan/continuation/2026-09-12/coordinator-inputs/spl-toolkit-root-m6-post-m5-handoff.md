# M6 handoff preparation — not an implementation release

The existing goal, scope, approved M6 design and eight-task sequence remain in force. Resume the existing `/root/milestone6_xhigh` coordinator only after root explicitly accepts final M5. This note prepares that bounded handoff; it does not establish M5 acceptance or authorize production work now.

Read current `/Users/jacobdelgado/.codex/AGENTS.md` and `/Users/jacobdelgado/.codex/skills/efficient-delegation/SKILL.md` before future delegations. Protected work requires equal-or-higher capability than root's Astra/XHigh assignment; explicit `gpt-6-astra`, `xhigh`, non-overriding default/worker role and isolated context is the conservative choice. Do not use retired High coordinators or lower-capability fixed reviewer roles. Preserve others' edits, raw failures/warnings and exact artifact receipts.

Retain the accepted adoption receipt at `/private/tmp/spl-toolkit-m6-xhigh-adoption/adoption-receipt.md` and the approved preflights `/private/tmp/spl-toolkit-m6-core-preflight.md`, `/private/tmp/spl-toolkit-m6-cli-rest-preflight.md`, and `/private/tmp/spl-toolkit-m6-native-package-preflight.md`. Reconcile only relevant intervening changes against the final M5 acceptance commit. Integrate the approved proposals into the existing ExecPlan, then return its exact path/hash and a concise delta for root's plan release. Do not restart brainstorming or repeat unchanged preflights.

Current M5 review candidate is `fcbb74ac60da312bc06275e4e3de29b570a3bf2e`, implementation `48d8012286dbfa2467717c6046d0a79e9ab589e3`. It is still under review and may be superseded. Final source authority is the root-accepted commit, not this provisional identifier. Candidate evidence is `docs/evidence/milestone-5/verification.json`; the final section of `.superpowers/sdd/milestone-5-implementation-plan/task-11-report.md` qualifies earlier historical notes and precommit binary VCS metadata.

The M5 Task11 handoff supplies a canonical atomic `data_model` dependency for tstats DATAMODEL_NAME. M6 consumes it and adds private render ownership/eligibility; do not duplicate extraction or split the atom into model/dataset/path identities. Static loadjob SID has a nonconsuming `search_job` reference but is not a new mapping-rule kind. Deferred output intentions do not establish field state or rewrite permission. Qualified join reads remain whole indeterminate identities; never invent root or suffix fields. Preserve canonical null_test and typed dotted-identity policy.

Capability additions may add rewrite support for both dialects while preserving existing values; whole-response byte identity is not required for an intentionally additive capability. A rewrite capability's role describes a stable documented rendering context, not merely generic read/create or an ANTLR type. Existing strict query/target/fields decoders, Python refusal category fixes, owned native result lifetimes and actual generated ABI headers remain authoritative.

Task11 now makes SPL2 surface acceptance mandatory on source and both strictly offline installed package paths. Preserve full-Go transport artifact identity, copied fixtures and maintained documentation, required nonzero test-module registration and zero-skip checks when adding rewrite suites. Preserve the final accepted counts as predecessor evidence without hardcoding them as a substitute for actual execution. Refresh both whole ABI headers from real Go1.22/Go1.25 outputs for new exports.

Root windows remain after Task5 (core acceptance and immutable Go baseline) and after Task8 (built/native/HTTP/Python3.14 final acceptance plus broad read-only review). No merge, push, publication, destructive cleanup, provider action or change to user-owned original-checkout files is authorized by this handoff.
