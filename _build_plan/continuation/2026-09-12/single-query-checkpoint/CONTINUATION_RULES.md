# Delegation rules retained for the next session

The user's replacement AGENTS.md message is authoritative and replaces earlier AGENTS instructions. Its protected-capability requirement persists through this handoff. The disk file `/Users/jacobdelgado/.codex/AGENTS.md` currently contains older instructions; do not use that difference to weaken the user's requirement.

The efficient-delegation skill was read in full earlier in this goal at `/Users/jacobdelgado/.codex/skills/efficient-delegation/SKILL.md` (then SHA256 `2b0c89a340dafe57c5af827aa24476e7e0371de8b7a3d4dbfac275c3482e77e7`). That file is missing as of the September 12 handoff. Narrow searches of the standard skill locations also found no replacement. The following retained rules reflect the read skill and the user's explicit replacement instructions; this is a continuation record, not a claim that the missing global file still exists.

- Preserve correctness and task scope. Delegate bounded work only when its benefit justifies setup and handoff overhead. Keep tiny known-file reads and simple deterministic extraction local; batch related mechanical work and avoid duplicate scans.
- Protected debugging, architecture, safety/security-sensitive changes and other high-consequence judgments require equal or higher capability than the coordinator. Match model and reasoning effort when equivalence is uncertain, and check role overrides. A cheaper model's effort label or a specialist name is not evidence of equivalence. Retain protected work locally if no qualifying worker is available.
- This goal used the conservative floor **gpt-6-astra / xhigh / default or worker role without an override**. Reassess against the fresh coordinator's actual capability; do not lower the floor silently. Specify both model and effort, or use documented inheritance with a non-overriding role when parent settings are unknown. Assigned settings are not backend execution attestation.
- Use isolated minimal context (`fork_turns: none` when appropriate), exact ownership, artifact paths, acceptance checks and concise evidence-backed returns. Workers share files and must preserve others' edits. Reuse an existing worker for related follow-ups; do not recurse without a separate reason and authorization.
- Keep raw stdout/stderr, command identity, actual exit status, failures and warnings recoverable. Do not infer success from silence, filter away inconvenient failures, or rerun side effects only to recreate summaries. For contradictory output, inspect the relevant raw windows and state the uncertainty.
- Resolve missing context/access/capability or take over locally instead of repeating an unchanged failed dispatch. Do not restart a worker solely because a wait timed out; inspect its authoritative live state.
- Use economical models for eligible routine work only. Preserve explicit user model choices. Claim savings only from measurements that include worker and retry overhead; context size, tokens, latency, spend and allowance are different measurements.
- Preserve the original objective and approvals, use the existing ExecPlans, make surgical changes and verify concrete outcomes. Do not merge, push, publish, delete user work, buy credits or consume resets without authorization.

The September 12 single-query checkpoint reviewer was assigned `gpt-6-astra`, `xhigh`, `default`, `fork_turns: none`, with no children. This matches the retained protected floor. No active assignment needs a capability correction.
